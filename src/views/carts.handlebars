<div class="px-4 sm:px-6 lg:px-8">
  <div class="sm:flex sm:items-center">
    <div class="sm:flex-auto">
      <h1 class="text-base font-semibold leading-6 text-white">Carts</h1>
      <p class="mt-2 text-sm text-white">Here you will find all the
        <strong class="font-semibold text-white">Carts</strong>
        that are stored in our database.</p>
    </div>
    <div class="mt-4 sm:ml-16 sm:mt-0 sm:flex-none">
      <button
        type="button"
        id="btnAddCartById"
        class="block rounded-md bg-indigo-600 px-3 py-2 text-center text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 mb-4"
      >Add cart by id</button>
      <button
        type="button"
        id="btnAddNewCart"
        class="block rounded-md bg-indigo-600 px-3 py-2 text-center text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
      >Add new cart</button>
    </div>
  </div>

  <div id="carts">
    <div class="flex items-center justify-center h-3/5 w-full">
      <span class="text-4xl text-white">LOADING...</span>
    </div>
  </div>
  {{! <div class="-mx-4 mt-10 ring-1 ring-gray-300 sm:mx-0 sm:rounded-lg">
    <h2>Cart N°${carts.length}</h2>
    <table class="min-w-full divide-y divide-gray-300">
      <thead>
        <tr>
          <th
            scope="col"
            class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-6"
          >Productos</th>
          <th
            scope="col"
            class="hidden px-3 py-3.5 text-left text-sm font-semibold text-gray-900 lg:table-cell"
          >Id</th>
          <th
            scope="col"
            class="hidden px-3 py-3.5 text-left text-sm font-semibold text-gray-900 lg:table-cell"
          >Quantity</th>
          <th
            scope="col"
            class="hidden px-3 py-3.5 text-left text-sm font-semibold text-gray-900 lg:table-cell"
          >Code</th>
          <th
            scope="col"
            class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900"
          >Price</th>
          <th scope="col" class="relative py-3.5 pl-3 pr-4 sm:pr-6">
            <span class="sr-only">Delete</span>
          </th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td
            class="relative py-4 pl-4 pr-3 text-sm sm:pl-6 border-t border-transparent"
          >
            <div class="font-medium text-gray-900">
              ${aca va el nombre del producto}
            </div>
            <div class="mt-1 flex flex-col text-gray-500 sm:block lg:hidden">
              <span>
                ${aca va el quantity del producto}
              </span>
              <span class="hidden sm:inline">·</span>
              <span>
                ${aca va el code del producto}
              </span>
            </div>
            <div class="absolute -top-px left-6 right-0 h-px bg-gray-200"></div>
          </td>
          <td
            class="hidden px-3 py-3.5 text-sm text-gray-500 lg:table-cell border-t border-gray-200"
          >
            ${aca va el id del producto}
          </td>
          <td
            class="hidden px-3 py-3.5 text-sm text-gray-500 lg:table-cell border-t border-gray-200"
          >
            ${aca va el quantity del producto}
          </td>
          <td
            class="hidden px-3 py-3.5 text-sm text-gray-500 lg:table-cell border-t border-gray-200"
          >
            ${aca va el code del producto}
          </td>
          <td
            class="px-3 py-3.5 text-sm text-gray-500 border-t border-gray-200"
          >
            <div class="sm:hidden">
              ${aca va el price del producto}
            </div>
            <div class="hidden sm:block">
              ${aca va el price del producto}
            </div>
          </td>
          <td
            class="relative py-3.5 pl-3 pr-4 text-right text-sm font-medium sm:pr-6"
          >
            <button
              type="button"
              class="inline-flex items-center rounded-md bg-white px-2.5 py-1.5 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50 disabled:cursor-not-allowed disabled:opacity-30 disabled:hover:bg-white"
            >Delete<span class="sr-only">, Hobby</span></button>
          </td>
        </tr>
      </tbody>
    </table>
  </div> }}
</div>

<script src="/socket.io/socket.io.js"></script>

<script src="/js/carts.js"></script>